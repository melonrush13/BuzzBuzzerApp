<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.isaac.buzzbuzzerapp">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Warning">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signUp" />
        <activity android:name=".LandingPage" />
        <activity android:name=".Login" />
        <activity android:name=".Hosting" />
        <activity android:name=".Joining" />
        <activity android:name=".Party">
        </activity>
        <!-- GTIS service allows listening for geofence transitions-->
        <service android:name="com.isaac.buzzbuzzerapp.GeofenceTransitionsIntentService"/>
    </application>

</manifest>